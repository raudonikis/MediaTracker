<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="bottomNavigationStyle">
            @style/Widget.MaterialComponents.BottomNavigationView.Colored
        </item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Spacing -->
        <item name="spacingSmall">@dimen/spacing_small</item>
        <item name="spacingNormal">@dimen/spacing_normal</item>
        <item name="spacingMedium">@dimen/spacing_medium</item>
        <item name="spacingLarge">@dimen/spacing_large</item>
    </style>

</resources>
